services:
  notes:
    build: .
    container_name: stillnote
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PORT:-8080}:8080"
    volumes:
      - notes-data:/app/data

volumes:
  notes-data:
